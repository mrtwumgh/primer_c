#include <stdio.h>


int main(void)
{
	int toes = 10;
	int twice = toes + toes;
	int squared = toes * toes;

	printf("toes: %d twice is %d and toes squared is %d\n", toes, twice, squared);

	return (0);
}
